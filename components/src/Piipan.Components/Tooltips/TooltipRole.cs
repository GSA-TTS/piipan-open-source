namespace Piipan.Components.Tooltips
{
    public enum TooltipRole
    {
        Label, // Use if the screenreader should read the tooltip as if it was the text of the item showing the tooltip
        Description // Use if the screenreader should use this text as additional information for the item showing the tooltip
    }
}

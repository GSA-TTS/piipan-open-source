@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@using Piipan.Notification.Common.Models
@{

    @model MatchEmailModel

}
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <style>
        a {
            color: #005EA2 !important;
        }

        .usa-button a {
            text-decoration: none !important;
        }

        .usa-button {
            font-family: Source Sans Pro Web,Helvetica Neue,Helvetica,Roboto,Arial,sans-serif;
            font-size: 17px;
            line-height: .9;
            color: #fff !important;
            background-color: #005ea2 !important;
            -webkit-appearance: none;
            -moz-appearance: none;
            appearance: none;
            border: 0;
            border-radius: 4px;
            cursor: pointer;
            display: inline-block;
            font-weight: 700;
            mso-line-height-rule: exactly;
            mso-text-raise: 6px;
            line-height: 40px;
            text-decoration: none !important;
        }

        body {
            font-family: Source Sans Pro Web,Helvetica Neue,Helvetica,Roboto,Arial,sans-serif;
            font-size: 15px;
        }
    </style>
</head>
<body>
    <p>This is an automated notification and we are unable to accept replies. If you need additional assistance, please contact us at <a href="mailto:@(Model.ReplyToEmail)?subject=NAC%20Match%20@(Model.MatchId)">@Model.ReplyToEmail</a>.</p>
    <p>A NAC match has been found with your state.</p>
    <p>
        <strong>Initiating State:</strong> @Model.InitState
        <br>
        <strong>Matching State:</strong> @Model.MatchingState
        <br>
        <strong>NAC Match ID: </strong><a href="@Model.MatchingUrl" title="View Match">@Model.MatchId</a>
    </p>
    <p><em>Initial action to resolve this match must be taken and communicated to the other State agency <strong>by @Model.InitialActionBy.ToShortDateString()</strong>. Updating the initial action information in the NAC match record will generate an automated notification to the other State agency.</em></p>

    <p style="margin-bottom: 5px;">Click on the NAC Match ID link to go to the match record.</p>
    <table>
        <tbody>
            <tr>
                <td class="usa-button">
                    <a href="@Model.MatchingUrl" class="usa-button" title="View Match">&nbsp;&nbsp;&nbsp;&nbsp;View NAC Match ID @Model.MatchId&nbsp;&nbsp;&nbsp;&nbsp;</a>
                </td>
            </tr>
        </tbody>
    </table>

    <p>Use the NAC Match ID to communicate with @(Model.IsInitiatingState ? Model.MatchingState : Model.InitState) about this match. <strong>Do not share PII over email.</strong></p>
    <hr>
    <p>
        <a href="@Environment.GetEnvironmentVariable("QueryToolUrl")" title="Go To NAC Main Page"><img src="cid:usda-img" alt="USDA National Accuracy Clearinghouse" /></a>
    </p>

    <p> This electronic message contains information generated by the USDA solely for the intended recipients. Any unauthorized interception of this message or the use or disclosure of the information it contains may violate the law and subject the violator to civil or criminal penalties. If you believe you have received this message in error, please notify the sender and delete the email immediately.</p>
</body>
</html>
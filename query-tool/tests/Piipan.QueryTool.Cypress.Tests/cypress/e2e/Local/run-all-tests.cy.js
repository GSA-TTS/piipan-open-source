// When running locally, it can be a pain if you want to see if any tests are broken. Rather than running each spec file individually in the
// Cypress test runner, you can run this to run them all at once.

import '../CICD/match-list.cy.js'
import '../CICD/homepage.cy.js'
import '../CICD/match-query.cy.js'
import '../CICD/participant-query.cy.js'